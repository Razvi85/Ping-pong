# Ping Pong App

## Instalare
1. Clonează repo:
   git clone <repo-url>
2. Intră în folder:
   cd ping_pong_app
3. Instalează dependențele Flutter:
   flutter pub get
4. Rulează aplicația:
   flutter run
5. Build APK:
   flutter build apk --release
